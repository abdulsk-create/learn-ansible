- name: demo on loops
  hosts: localhost
  connection: local
  vars:
    fruits:
      - orange
      - apple
  tasks:
    - name: print fruits
      ansible.builtin.debug:
        msg: Fruit Name - "{{ item }}"
        loop: "{{ fruits }}"
